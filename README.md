# zed-vixen

Zed language support for Vixen.

The extension points at the in-repo `crates/tree-sitter-vixen` grammar. When
publishing or testing from a committed branch, update the `rev` in
`extension.toml` to a commit that contains the current grammar.
